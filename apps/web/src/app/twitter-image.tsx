// The X/Twitter share card reuses the Open Graph generator (same 1200×630
// branded card). Kept as a separate file so Next emits the twitter:image tag.
export { default, alt, size, contentType } from "./opengraph-image";
